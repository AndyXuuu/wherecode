# MB4 Go/No-Go Draft

Generated: 2026-03-06T13:49:12Z

## Decision Scope

- Decision target: local single-host release readiness for the main workflow engine.
- Out of scope: multi-host HA deployment, cloud-scale SLO, production traffic hardening.

## Entry Criteria Check

- `MB3` full-loop evidence exists: PASS (`docs/ops_reports/20260306T134242Z-mb3-t5-full-loop.json`).
- MB3 dry-run latest pointer valid: PASS (`docs/ops_reports/latest_mb3_dry_run_seed.json`).
- `MB4-T1` release gate baseline green: PASS (`bash scripts/check_all.sh release`).
- Recovery path available in API: PASS (`/v3/workflows/runs/{run_id}/orchestrate/recover`).

## Recommendation

- Decision: **GO-CANDIDATE** (local/single-host release).
- Gate type: conditional go.

## Go Conditions

- Keep `WHERECODE_DECOMPOSE_ALLOW_SYNTHETIC_FALLBACK=true` in local runtime.
- Keep manual confirmation step for decomposition review in operator workflow.
- Run pre-launch gate once per release cut: `bash scripts/check_all.sh release`.

## Non-Blocking Caveats

- External provider failures still possible; fallback mitigates availability but not output quality drift.
- Downstream workitem execution outcomes depend on provider/runtime health and prompt quality.

## Immediate Next Step

- Move to MB5 launch-decision checkpoint with acceptance summary based on this draft.
