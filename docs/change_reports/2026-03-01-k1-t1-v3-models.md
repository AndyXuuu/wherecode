# Change Report: K1-T1 v3 runtime models

Date: 2026-03-01

## 1. Goal

- Implement K1-T1 from v3 backlog.
- Add runtime models for workflow orchestration and basic validation tests.

## 2. Plan update

- Updated `PLAN.md`:
  - marked K1-T1 started
  - marked K1-T1 completed with verification commands
- Updated `docs/v3_task_board.md`:
  - changed K1-T1 status from `doing` to `done`

## 3. Implementation

- Added `control_center/models/v3_workflow.py`:
  - enums: `WorkflowRunStatus`, `WorkItemStatus`, `GateStatus`, `DiscussionStatus`
  - enums: `GateType`, `ArtifactOwnerType`, `ArtifactType`
  - models: `WorkflowTemplate`, `WorkflowRun`, `WorkItem`, `GateCheck`, `DiscussionSession`, `Artifact`
  - constraints:
    - `discussion_used <= discussion_budget`
    - timestamp order validation
    - dependency deduplication
    - discussion budget/resolve constraints
- Updated `control_center/models/__init__.py`:
  - exported all new v3 model classes and enums
- Added `tests/unit/test_v3_workflow_models.py`:
  - 7 tests covering ids and validation rules

## 4. Verification

- `control_center/.venv/bin/pytest -q tests/unit/test_v3_workflow_models.py`
  - result: `7 passed`
- `control_center/.venv/bin/pytest -q tests/unit`
  - result: `59 passed`

## 5. Risks / follow-up

- Current models are storage/domain level only; API contracts for v3 are not added yet.
- Next task: K1-T2 (`role -> executor` registry), then K1-T3 (`agent.md` isolation loader).
