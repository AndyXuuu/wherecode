# Legacy Role Profiles (Compatibility)

This directory is kept for backward compatibility.

Source of truth moved to:

- `.agents/roles/<role>/AGENTS.md`

Action Layer loader default search order:

1. `.agents/roles`
2. `action_layer/agents` (legacy fallback)

