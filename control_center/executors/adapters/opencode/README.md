# OpenCode Adapter (Scaffold)

Planned responsibilities:
- invoke OpenCode CLI/runtime from `external/executors/opencode`
- normalize task execution response into WhereCode executor contract
- expose compatibility metadata and tested version/tag

